@model IEnumerable<Chico.Models.OrgPerson>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.SharesOwned)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.AffiliationStartDate)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Org)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrgRole)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Person)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SharesOwned)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AffiliationStartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Org.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrgRole.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Person.FirstName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
